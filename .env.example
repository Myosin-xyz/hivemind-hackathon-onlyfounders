# Hivemind — REQUIRED
HIVEMIND_API_KEY=
HIVEMIND_API_URL=https://hivemind.myosin.xyz

# Beacon — REQUIRED for gap analysis (niche patterns)
# Skip these if Beacon is not deployed; gap analysis degrades gracefully
BEACON_API_KEY=
BEACON_API_URL=

# Anthropic — for the "generic Claude" baseline in the three-way blind test
# Used only by /api/baseline (the A in the A/B/C comparison)
ANTHROPIC_API_KEY=
